# cinemapedia

A new Flutter project.

# DEV
1. Copiar el .env.template y renombrarlo a .env
2. Cambiar las variables de entorno (The MovieDB)
3. Ejecutar el comando para la base de datos local
```
flutter pub run build_runner build
```